using System.Collections;
using System.Collections.Generic;
using System.Drawing;
using UnityEngine;
using UnityEngine.UI;

public class AnalogAdjustScripts : MonoBehaviour
{
    /*public Slider slider;
    public GameObject analog;
    public float size;
    
  
    public void OnSizeChanged(float _newSize)
    {
        Vector3 newsize = new Vector3(slider.value, slider.value, slider.value);
        Debug.Log(newsize);
        size = slider.value;
        analog.transform.localScale = newsize;

        PlayerPrefs.SetFloat("newSize",size);
    }*/
}